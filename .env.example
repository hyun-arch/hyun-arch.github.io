# 이 파일을 `.env` 로 복사해 값을 채우세요. (.env 는 커밋되지 않습니다)

# 관리자 콘솔(/admin) 편의 잠금 암호.
# ⚠️ 정적 사이트라 이 값은 빌드 번들에 들어가 소스로 보일 수 있는 "편의 잠금"입니다.
#    온라인 실보안이 아니며, 진짜 잠금은 Supabase Auth 로그인(다음 슬라이스)입니다.
PUBLIC_ADMIN_PASS=바꾸세요

// @ts-check
import { defineConfig } from 'astro/config';

// Aramirror static site. Add MDX/integrations later as the content grows.
export default defineConfig({
  site: 'https://aramirror.com',
});
